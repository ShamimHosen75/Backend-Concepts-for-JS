const colors = required('colors');

// Start

console.log("Welcome to ACC!");
setTimeout(() =>{
  const user = {id: 1};

  setTimeout(() => {
    const product = [{}, {}, {}];

    setTimeout(() => {
      const prices = [];
    },1000)
  }, 500);
}, 1000);

console.log("I'm excited!"); 